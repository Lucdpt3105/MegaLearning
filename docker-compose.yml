services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      APP_NAME: MegaLearning
      APP_ENV: production
      APP_KEY: base64:W0vhEBMGD4YiM1ssc+CqSO1u8YRZmkwzD1IpzkWDluU=
      APP_DEBUG: "false"
      APP_URL: http://localhost:8080
      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: megalearning
      DB_USERNAME: megalearning
      DB_PASSWORD: secret
      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database
      TELESCOPE_ENABLED: "false"
      LOG_CHANNEL: stderr

      # Supabase Auth (Google OAuth)
      SUPABASE_URL: https://ncymmvezqqibpeifvdtl.supabase.co
      SUPABASE_ANON_KEY: sb_publishable_-IK4R3w4RdNoeEOVg4oCNg_ZXk-f4lN
      SUPABASE_SERVICE_ROLE_KEY: ""

      # Gemini AI
      GEMINI_API_KEY: AIzaSyA-QxSigqfMdIkxxml3cUX-TknanxkBT7A
      GEMINI_MODEL: gemini-2.0-flash-exp
      AI_NAME: "AI Assistant"
      GEMINI_MAX_TOKENS: "500"

      # Zoom
      ZOOM_ACCOUNT_ID: 0KM705emTZasJolW2fV7Cw
      ZOOM_CLIENT_ID: 6hpv4p7OQm2tseblMJAjiQ
      ZOOM_CLIENT_SECRET: Gf1arzBazP1MqoyzagRvvdW6ffcxpvtA
      ZOOM_SECRET_TOKEN: NfGPCiKORoehGQdZuch-Qw

      # Mail
      MAIL_MAILER: smtp
      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: "587"
      MAIL_USERNAME: lucksnow1108@gmail.com
      MAIL_PASSWORD: friuxuoucdzbashk
      MAIL_ENCRYPTION: tls
      MAIL_FROM_ADDRESS: lucksnow1108@gmail.com
      MAIL_FROM_NAME: MegaLearning
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootsecret
      MYSQL_DATABASE: megalearning
      MYSQL_USER: megalearning
      MYSQL_PASSWORD: secret
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  mysql_data:
